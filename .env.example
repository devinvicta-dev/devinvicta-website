# Resend — get your key at https://resend.com/api-keys
RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Contact form routing (optional — sensible defaults are used if unset).
# Set CONTACT_FROM to a verified-domain sender once devinvicta.com is verified
# in Resend; until then the Resend sandbox sender (onboarding@resend.dev) is used.
CONTACT_TO=info@devinvicta.com
CONTACT_FROM="DevInvicta <onboarding@resend.dev>"
